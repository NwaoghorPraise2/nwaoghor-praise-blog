const array =  [1, 2, 3, 4, 5]
const lenth_array_half = array.length / 2
const half = array[4]

let filtered = array.filter((x)=> x > 2);

// console.log(filtered);

let flt = array.slice(0, 3);

console.log(flt);